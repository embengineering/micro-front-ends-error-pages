{
  "name": "micro-front-ends-error-pages",
  "description": "Simple 404 Not Found error page using a web component and a React component",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://embengineering.com/micro-front-ends-error-pages",
  "dependencies": {
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
